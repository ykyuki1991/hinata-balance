import {chromium} from '@playwright/test';
import fs from 'node:fs/promises';
import assert from 'node:assert/strict';
const browser=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});
const results=[];await fs.mkdir('.cache/public-live',{recursive:true});
try{for(const [mode,seed,strategy] of [['NORMAL',42,'C']]){
 const page=await browser.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true});await page.addInitScript(seed=>{Math.random=()=>{seed=(seed*1664525+1013904223)>>>0;return seed/4294967296;};},seed);await page.goto('https://ykyuki1991.github.io/hinata-balance/?debug=true');await page.addScriptTag({path:'.cache/qa-planner.js'});await page.getByRole('button',{name:new RegExp(mode)}).click();const placements=[];
 for(let n=0;n<27;n++){
  const plan=await page.evaluate(async(strategy)=>{const g=window.__hinataGame;if(g.phase==='ended')return null;const {choose}=window.HinataQA;return {choice:choose(g,strategy),pos:g.pos,camera:g.camera,member:g.queue[g.count].name};},strategy);if(!plan)break;
  const degrees=Math.round(plan.choice.angle*180/Math.PI);for(let k=0;k<Math.abs(degrees)/15;k++)await page.getByRole('button',{name:degrees<0?'左へ回転':'右へ回転'}).click();
  const live=await page.evaluate(()=>({pos:window.__hinataGame.pos,camera:window.__hinataGame.camera}));const b=await page.locator('canvas').boundingBox();const xy=(x,y)=>({x:b.x+x/390*b.width,y:b.y+(y+live.camera)/650*b.height});const from=xy(live.pos.x,live.pos.y),to=xy(plan.choice.x,plan.choice.y);
  await page.mouse.move(from.x,from.y);await page.mouse.down();await page.mouse.move(to.x,to.y,{steps:8});await page.mouse.up();await page.waitForFunction(()=>window.__hinataGame.phase!=='settling',null,{timeout:18000});await page.waitForTimeout(600);
  const state=await page.evaluate(()=>{const g=window.__hinataGame;return {count:g.count,phase:g.phase,score:g.score,tilt:g.platform.angle*180/Math.PI,reason:g.failureReason,order:g.queue.map(m=>m.id),seconds:g.seconds,hint:g.hint};});placements.push({...plan.choice,member:plan.member,...state});await page.screenshot({path:`.cache/public-live/${mode.replaceAll(' ','')}-${seed}-${strategy}-${n}.png`});if(state.phase==='ended'||state.hint)break;
 }
 const result={mode,seed,strategy,placements};results.push(result);console.log(JSON.stringify({mode,seed,strategy,...placements.at(-1)}));await fs.writeFile('.cache/public-live/results.json',JSON.stringify(results,null,2));await page.getByRole('heading',{name:'CLEAR!'}).waitFor();
 const order=placements.at(-1).order;const best=placements.at(-1).score;const started=Date.now();await page.getByRole('button',{name:/もう一度遊ぶ/}).click();await page.locator('canvas').waitFor();assert.ok(Date.now()-started<2000);assert.deepEqual(await page.evaluate(()=>window.__hinataGame.queue.map(m=>m.id)),order);
 const box=await page.locator('canvas').boundingBox();await page.mouse.move(box.x+195/390*box.width,box.y+90/650*box.height);await page.mouse.down();await page.mouse.move(box.x+370/390*box.width,box.y+450/650*box.height,{steps:8});await page.mouse.up();await page.getByRole('heading',{name:'GAME OVER'}).waitFor({timeout:18000});assert.match(await page.locator('.result').innerText(),/落下/);
 await page.reload();const record=await page.evaluate(()=>JSON.parse(localStorage.getItem('hinata-records')).NORMAL);assert.equal(record.clears,1);assert.equal(record.plays,2);assert.equal(record.bestScore,best);await page.getByRole('button',{name:/RECORD/}).click();assert.match(await page.locator('.record-card').first().innerText(),new RegExp(String(best)));console.log(JSON.stringify({publicClear:true,fall:true,retryUnder2s:true,sameOrder:true,recordAfterReload:record}));await fs.writeFile('docs/public-play-check.json',JSON.stringify({base:'https://ykyuki1991.github.io/hinata-balance/',publicClear:true,fall:true,retryUnder2s:true,sameOrder:true,recordAfterReload:record,placements},null,2));await page.close();
}}finally{await browser.close();}
